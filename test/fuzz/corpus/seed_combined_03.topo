// Stages + visibility + hints
using Int = std::cpp17::int;
namespace engine {
  public:
    void run();
    fn run {
      stage<1> init();
      stage<2> process();
      stage<3> cleanup();
    }
    process(Array items) -> void, cardinality(10k..100k), access(streaming);

  protected:
    void init();
    void cleanup();

  private:
    void helper();
}
