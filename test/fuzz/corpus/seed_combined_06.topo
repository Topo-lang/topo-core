// Priority + visibility + pipeline
namespace svc {
  public:
    priority(critical):
      void start();
    priority(high):
      void process();
    priority(low):
      void log();

    fn start {
      init -> ready;
      ready -> void;
    }
  protected:
    void init();
    void ready();
}
