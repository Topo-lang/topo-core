// Multi-return in pipeline
using Int = std::cpp17::int;
using Float = std::cpp17::float;
namespace pipe {
  public:
    void run();
    fn run {
      compute -> report;
      report -> void;
    }
  protected:
    compute(Int n) -> (Float a, Float b);
    void report();
}
