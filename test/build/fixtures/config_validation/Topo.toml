[project]
name = "config_validation"

[topo]
root = "topo/main.topo"

[build]
sources = ["src/main.cpp"]
standard = "c++17"
output = "config_validation"

# Deliberately invalid: adaptive requires embed_ir and parallel.instrument
[adaptive]
enabled = true
